import Nav from "./NavBar";
import '../styles/Header.css'
import Hero from "./Hero";

const Header = () => {
  return (
  <header>
    <Nav />
    <Hero />
  </header>

  );
};

export default Header;
